export const colors = {
  primary: '#E50914',
  background: { dark: '#141414', light: '#FFFFFF' },
  surface: { dark: '#1F1F1F', light: '#F5F5F5' },
  text: { primary: '#FFFFFF', secondary: '#B3B3B3', muted: '#666666' },
} as const

export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
} as const

export const radius = {
  sm: 4,
  md: 8,
  lg: 12,
  xl: 16,
} as const
